exports.handler=async function(event){if(event.httpMethod!=='POST')return{statusCode:405,body:JSON.stringify({error:'Method not allowed'})};const apiKey=process.env.GEMINI_API_KEY;if(!apiKey)return{statusCode:500,body:JSON.stringify({error:'GEMINI_API_KEY is not configured in Netlify.'})};let body;try{body=JSON.parse(event.body||'{}')}catch{return{statusCode:400,body:JSON.stringify({error:'Invalid JSON body.'})}}const endpoint=`https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${apiKey}`;let prompt='';if(body.mode==='criteria'){prompt=`You are a procurement evaluation expert. From the scope below, create JSON only with an array named criteria. Each item must have id, type, parameter, maxMark, guidance. Type must be Technical or Functional. Scope:\n${body.scope}`}else if(body.mode==='score'){prompt=`You assist a procurement evaluation committee. Score the vendor response against each criterion. Return JSON only with an array named scores. Each item must have criterionId, parameter, maxMark, aiScore, justification, evidence. aiScore must be numeric from 0 to maxMark. Do not invent compliance. If evidence is absent, score low and say evidence not found.\nCriteria:\n${JSON.stringify(body.criteria)}\nVendor:${body.vendorName}\nVendor Response:\n${body.vendorText}`}else{return{statusCode:400,body:JSON.stringify({error:'Unsupported mode.'})}}try{const response=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({contents:[{parts:[{text:prompt}]}],generationConfig:{temperature:.2}})});const data=await response.json();if(!response.ok)return{statusCode:response.status,body:JSON.stringify({error:data.error?.message||'Gemini API error'})};let text=data.candidates?.[0]?.content?.parts?.[0]?.text||'';text=text.replace(/```json|```/g,'').trim();let parsed;try{parsed=JSON.parse(text)}catch{return{statusCode:200,body:JSON.stringify({raw:text,criteria:[],scores:[]})}}return{statusCode:200,body:JSON.stringify(parsed)}}catch(err){return{statusCode:500,body:JSON.stringify({error:err.message})}}};